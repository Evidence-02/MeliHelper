local backdrop = {}

backdrop.name = "MeliHelper/StarCitizenBackdrop"
backdrop.canBackground = true
backdrop.defaultData = {
	only ="*", exclude="", flag = "", notflag = "", stars = 40, widthMin = 12, widthMax = 32
}
backdrop.fieldOrder = {
    "texture", "only", "exclude", "tag", "flag", "notflag", "stars", "widthMin", "widthMax"
}

backdrop.fieldInformation = {
    stars = {
        fieldType = "integer",
        minimumValue = 1
    }
}

return backdrop